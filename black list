-- ============================================================================
-- 🔮 YOKAI.WIN UI SYSTEM - ULTIMATE REPLICATION FIXED SCRIPT
-- ============================================================================

task.spawn(function()
	local success, bootErr = pcall(function()
		local Players = game:GetService("Players")
		local LocalPlayer = Players.LocalPlayer
		local Lighting = game:GetService("Lighting")
		local TweenService = game:GetService("TweenService")
		local UserInputService = game:GetService("UserInputService")
		local SoundService = game:GetService("SoundService")
		local HttpService = game:GetService("HttpService")

		-- 二重起動によるクラッシュを防ぐため古いUIを強制削除
		local oldGui = LocalPlayer:WaitForChild("PlayerGui"):FindFirstChild("BL_CyberGui")
		if oldGui then oldGui:Destroy() end

		local BlacklistIds = {}
		local selectedPlayer = nil

		-- 本物のファイル保存用Configデータ
		local CONFIG_FILE = "yokai_win_config.json"
		local currentSettings = {
			EspEnabled = false,
			YokaiWorldEnabled = false,
			AutoLoadEnabled = true,
			GlowTransparency = 71,
			ColorStrength = 100
		}

		-- ============================================================================
		-- 🔊 1. 0.5秒のタメを置いて完璧に繋がって喋るKJボイスシステム
		-- ============================================================================
		task.spawn(function()
			pcall(function()
				local s1 = Instance.new("Sound")
				s1.SoundId = "rbxassetid://18434079822" -- 「もう遊びは終わりだ」
				s1.Volume = 5
				s1.Parent = SoundService
				
				local s2 = Instance.new("Sound")
				s2.SoundId = "rbxassetid://14032128795" -- 「これですべてを終わらせる」
				s2.Volume = 5
				s2.Parent = SoundService

				if not s1.IsLoaded then s1.Loaded:Wait() end
				if not s2.IsLoaded then s2.Loaded:Wait() end

				s1:Play()
				s1.Ended:Connect(function()
					s1:Destroy()
					task.wait(0.5) -- 0.5秒の絶妙な「ため」
					s2:Play()
				end)
				s2.Ended:Connect(function() s2:Destroy() end)
			end)
		end)

		----------------------------------------------------------------
		-- 🧱 2. 画面の土台作成
		----------------------------------------------------------------
		local ScreenGui = Instance.new("ScreenGui", LocalPlayer.PlayerGui)
		ScreenGui.Name = "BL_CyberGui"
		ScreenGui.ResetOnSpawn = false

		local OpenButton = Instance.new("TextButton", ScreenGui)
		OpenButton.Size = UDim2.new(0, 70, 0, 35)
		OpenButton.Position = UDim2.new(1, -85, 0, 15)
		OpenButton.Text = "Open"
		OpenButton.TextSize = 14
		OpenButton.Font = Enum.Font.SourceSansBold
		OpenButton.TextColor3 = Color3.fromRGB(255, 255, 255)
		OpenButton.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
		OpenButton.Visible = false -- 最初は開いてるので隠す

		local OB_Corner = Instance.new("UICorner", OpenButton)
		OB_Corner.CornerRadius = UDim.new(0, 8)
		local OB_Stroke = Instance.new("UIStroke", OpenButton)
		OB_Stroke.Color = Color3.fromRGB(255, 255, 255)
		OB_Stroke.Thickness = 1.5

		local MainFrame = Instance.new("Frame", ScreenGui)
		MainFrame.Size = UDim2.new(0, 640, 0, 460)
		MainFrame.Position = UDim2.new(0.5, -320, 0.5, -230)
		MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
		MainFrame.BorderSizePixel = 0
		MainFrame.Visible = true -- 実行時に即時自動全開！
		Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

		local CloseButton = Instance.new("TextButton", MainFrame)
		CloseButton.Size = UDim2.new(0, 26, 0, 26)
		CloseButton.Position = UDim2.new(1, -34, 0, 8)
		CloseButton.Text = "X"
		CloseButton.TextSize = 14
		CloseButton.Font = Enum.Font.SourceSansBold
		CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
		CloseButton.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
		CloseButton.ZIndex = 10
		Instance.new("UICorner", CloseButton).CornerRadius = UDim.new(0, 5)

		----------------------------------------------------------------
		-- ☯️ 3. 左サイドバー（yokai.winロゴ配置）
		----------------------------------------------------------------
		local Sidebar = Instance.new("Frame", MainFrame)
		Sidebar.Size = UDim2.new(0, 85, 1, 0)
		Sidebar.BackgroundColor3 = Color3.fromRGB(11, 11, 13)
		Sidebar.BorderSizePixel = 0
		Instance.new("UICorner", Sidebar).CornerRadius = UDim.new(0, 10)

		local SidebarLine = Instance.new("Frame", Sidebar)
		SidebarLine.Size = UDim2.new(0, 1, 1, 0)
		SidebarLine.Position = UDim2.new(1, -1, 0, 0)
		SidebarLine.BackgroundColor3 = Color3.fromRGB(26, 26, 30)

		local ContentFrame = Instance.new("Frame", MainFrame)
		ContentFrame.Size = UDim2.new(1, -85, 1, 0)
		ContentFrame.Position = UDim2.new(0, 85, 0, 0)
		ContentFrame.BackgroundTransparency = 1

		local Pages = {}
		local function createPage(name)
			local p = Instance.new("Frame", ContentFrame)
			p.Size = UDim2.new(1, 0, 1, 0)
			p.BackgroundTransparency = 1
			p.Visible = false
			Pages[name] = p
			return p
		end

		createPage("combat")
		createPage("movement")
		createPage("visuals")
		createPage("sun")
		createPage("gear")
		createPage("save")

		----------------------------------------------------------------
		-- ☀️ 太陽タブ（4番目）：敵が緑に光る本物ESPシステム
		----------------------------------------------------------------
		local SunScroll = Instance.new("ScrollingFrame", Pages["sun"])
		SunScroll.Size = UDim2.new(1, -20, 1, -40)
		SunScroll.Position = UDim2.new(0, 10, 0, 20)
		SunScroll.BackgroundTransparency = 1
		SunScroll.BorderSizePixel = 0
		SunScroll.ScrollBarThickness = 3
		local SunLayout = Instance.new("UIListLayout", SunScroll)
		SunLayout.Padding = UDim.new(0, 8)

		local EspToggleFrame = Instance.new("Frame", SunScroll)
		EspToggleFrame.Size = UDim2.new(1, -10, 0, 40)
		EspToggleFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
		Instance.new("UICorner", EspToggleFrame).CornerRadius = UDim.new(0, 6)

		local EspLbl = Instance.new("TextLabel", EspToggleFrame)
		EspLbl.Size = UDim2.new(1, -60, 1, 0)
		EspLbl.Position = UDim2.new(0, 12, 0, 0)
		EspLbl.Text = "☐  Player ESP (Green Highlight)"
		EspLbl.TextColor3 = Color3.fromRGB(160, 160, 165)
		EspLbl.TextSize = 13
		EspLbl.Font = Enum.Font.SourceSansBold
		EspLbl.TextXAlignment = Enum.TextXAlignment.Left
		EspLbl.BackgroundTransparency = 1

		local EspBtn = Instance.new("TextButton", EspToggleFrame)
		EspBtn.Size = UDim2.new(1, 0, 1, 0)
		EspBtn.BackgroundTransparency = 1
		EspBtn.Text = ""

		local function updateEspVisuals(active)
			currentSettings.EspEnabled = active
			if active then
				EspLbl.Text = "☑  Player ESP (Green Highlight)"
				EspLbl.TextColor3 = Color3.fromRGB(160, 210, 20)
				task.spawn(function()
					while currentSettings.EspEnabled do
						for _, p in ipairs(Players:GetPlayers()) do
							if p ~= LocalPlayer and p.Character then
								local hl = p.Character:FindFirstChild("YokaiESP")
								if not hl then
									hl = Instance.new("Highlight")
									hl.Name = "YokaiESP"
									hl.FillColor = Color3.fromRGB(160, 210, 20) -- 鮮やかな緑発光！
									hl.FillTransparency = 0.4
									hl.OutlineColor = Color3.fromRGB(255, 255, 255)
									hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
									hl.Parent = p.Character
								end
							end
						end
						task.wait(1)
					end
				end)
			else
				EspLbl.Text = "☐  Player ESP (Green Highlight)"
				EspLbl.TextColor3 = Color3.fromRGB(160, 160, 165)
				for _, p in ipairs(Players:GetPlayers()) do
					if p.Character and p.Character:FindFirstChild("YokaiESP") then p.Character.YokaiESP:Destroy() end
				end
			end
		end
		box = Instance.new("TextButton") -- 代替ダミー
		EspBtn.MouseButton1Click:Connect(function() updateEspVisuals(not currentSettings.EspEnabled) end)

		----------------------------------------------------------------
		-- ⚙️ 歯車タブ（5番目）：【見本画像デザインを完全完全再現！】
		----------------------------------------------------------------
		local GearFrame = Pages["gear"]

		-- ① 左側：Default枠（Configボタン一覧）
		local LeftConfigBox = Instance.new("Frame", GearFrame)
		LeftConfigBox.Size = UDim2.new(0.5, -12, 1, -40)
		LeftConfigBox.Position = UDim2.new(0, 8, 0, 20)
		LeftConfigBox.BackgroundColor3 = Color3.fromRGB(20, 20, 22)
		local L_Stroke = Instance.new("UIStroke", LeftConfigBox)
		L_Stroke.Color = Color3.fromRGB(35, 35, 40)
		Instance.new("UICorner", LeftConfigBox).CornerRadius = UDim.new(0, 6)

		-- トップのドロップダウン風「Default」文字
		local DefaultHeader = Instance.new("TextLabel", LeftConfigBox)
		DefaultHeader.Size = UDim2.new(1, -16, 0, 32)
		DefaultHeader.Position = UDim2.new(0, 8, 0, 6)
		DefaultHeader.Text = "Default"
		DefaultHeader.TextColor3 = Color3.fromRGB(200, 200, 205)
		DefaultHeader.TextSize = 12
		DefaultHeader.Font = Enum.Font.SourceSansBold
		DefaultHeader.TextXAlignment = Enum.TextXAlignment.Left
		DefaultHeader.BackgroundColor3 = Color3.fromRGB(28, 28, 32)
		Instance.new("UICorner", DefaultHeader).CornerRadius = UDim.new(0, 4)

		-- 見本の縦並びのConfigボタン群を1パーツずつ配置
		local btnNames = {
			"Load config", "Save config", "Reset config", 
			"Import from clipboard", "Export to clipboard", 
			"Reset menu layout", "Lua script manager", "Unload"
		}
		
		local function createConfigButton(name, order)
			local b = Instance.new("TextButton", LeftConfigBox)
			b.Size = UDim2.new(1, -16, 0, 26)
			b.Position = UDim2.new(0, 8, 0, 150 + (order - 1) * 31)
			b.BackgroundColor3 = Color3.fromRGB(26, 26, 30)
			b.Text = name
			b.TextColor3 = Color3.fromRGB(160, 160, 165)
			b.TextSize = 11
			b.Font = Enum.Font.SourceSans
			Instance.new("UICorner", b).CornerRadius = UDim.new(0, 4)
			Instance.new("UIStroke", b).Color = Color3.fromRGB(40, 40, 45)
			return b
		end

		local ConfigButtons = {}
		for i, name in ipairs(btnNames) do
			ConfigButtons[name] = createConfigButton(name, i)
		end

		-- 【Config実作稼働】Save configボタンを押した時の処理
		ConfigButtons["Save config"].MouseButton1Click:Connect(function()
			pcall(function()
				if writefile then
					writefile(CONFIG_FILE, HttpService:JSONEncode(currentSettings))
					ConfigButtons["Save config"].Text = "Saved!"
					task.wait(1)
					ConfigButtons["Save config"].Text = "Save config"
				end
			end)
		end)

		-- 【Config実作稼働】Load configボタンを押した時の処理
		ConfigButtons["Load config"].MouseButton1Click:Connect(function()
			pcall(function()
